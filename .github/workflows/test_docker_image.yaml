name: Test Docker image

on:
  pull_request:
    paths-ignore:
      - 'deploy/charts/**'
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: pr-${{ github.event.pull_request.number || github.ref }}-docker-test
  cancel-in-progress: true

permissions:
  contents: read
  security-events: write

jobs:
  test:
    uses: OmniTrustILM/.github/.github/workflows/containers-test.yml@d262941d577f659a7e02914bde1af7143f46f28e # main
    with:
      image: ilm/cbom-lens
      cache-scope: cbom-lens
      build-args: |
        VERSION=${{ github.ref_name }}
